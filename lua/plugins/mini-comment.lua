-- Plugin configuration: mini-indentscope --------------------------------------------------

-- Disable `mini.comment` since the functionality is now in Neovim 0.10.
return {
  "mini.comment",
  enabled = false
}
