-- Plugin configuration: nvim-cmp ----------------------------------------------------------

return {
  "hrsh7th/nvim-cmp",

  opts = {
    completion = {
      -- Do not autocomplete on typing. Instead, wait for `<C-Space>`.
      autocomplete = false,
    },
  },
}
