-- Plugin configuration: which-key
-- ============================================================================

require("which-key").setup({
  plugins = {
    spelling = {
      enabled = true,
    },
  },
  window = {
    border = 'rounded',
  }
})
